# Python / packaging
.venv/
__pycache__/
*.pyc
*.egg-info/
dist/
build/

# Test / lint / coverage caches
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.mypy_cache/

# Editor / IDE / OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*.log

# Local env / secrets (no .env files in this repo today — defensive)
.env
.env.*
!.env.example

# Agent / AI-tooling per-project state
.claude/

# Data is gitignored — releases are produced fresh each run.
# The NHI website is the source of truth; re-fetch with `nhi-extract sync`.
data/regulations/medication/藥品給付規定_*/
data/regulations/medication/藥品給付規定_*.zip
data/regulations/medication/chapters/

# Pipeline-generated NHI release log (data lineage, not project history)
data/regulations/medication/CHANGELOG_data.md

# Local-only dev notes (HANDOFF, session state). Tracked by separate git repo inside.
.private/
