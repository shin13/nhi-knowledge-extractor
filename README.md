# nhi-knowledge-extractor

Convert Taiwan NHI medication regulation DOCX documents into RAG-ingestion-ready CSV deliverables.

See `docs/spec.md` for design.

## Quickstart

```bash
uv sync
uv run nhi-extract --help
```
